package com.nishchay.ds.linklist;

public class Client {

        // Driver Code
        public static void main(String[] args) {
/*            SinglyLinkedList list = new SinglyLinkedList();
            list.head = new SinglyLinkedList.Node(10);
            list.head.next = new SinglyLinkedList.Node(20);
            list.head.next.next = new SinglyLinkedList.Node(30);
            list.head.next.next.next = new SinglyLinkedList.Node(40);

            System.out.println("Given Linked list");
            list.printList(head);
            head = list.reverse(head);
            System.out.println("");
            System.out.println("Reversed linked list ");
            list.printList(head);*/

        }

}
